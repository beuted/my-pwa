<?xml version='1.0' encoding='utf-8'?>
<!--
  OPTION 1: No wildcard allow-navigation.
  Expected behavior on cordova-ios 6.1.0+:
    - The hidden iframe to bid.g.doubleclick.net is NOT allowed
    - The user gets KICKED OUT to Safari loading the iframe URL
    - External links (FedEx, UPS) correctly open in system browser
  This is the DEFAULT config — it demonstrates the bug.
-->
<widget id="com.example.whitelistbug" version="1.0.0"
        xmlns="http://www.w3.org/ns/widgets"
        xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>WhitelistBug</name>
    <description>Option 1: Strict allow-navigation (demonstrates the bug)</description>

    <content src="index.html" />

    <access origin="*" />

    <allow-navigation href="file://*" />

    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />

    <platform name="ios">
        <preference name="WKWebViewOnly" value="true" />
        <preference name="scheme" value="app" />
        <preference name="hostname" value="localhost" />
    </platform>

    <preference name="BackgroundColor" value="0xff222222" />
</widget>
